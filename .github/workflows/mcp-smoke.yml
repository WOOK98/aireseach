name: "CI / MCP Smoke"

on:
  schedule:
    # Every 6 hours, staggered from other workflows
    - cron: "17 */6 * * *"
  workflow_dispatch:
    inputs:
      force_run:
        description: "Force run even if recently executed"
        required: false
        default: "false"

concurrency:
  group: mcp-smoke
  cancel-in-progress: false

jobs:
  smoke:
    name: "🔍 MCP Smoke Test"
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: ✅ Checkout code
        uses: actions/checkout@v6

      - name: 🔍 Run MCP smoke test
        id: smoke
        env:
          MCP_SMOKE_ENDPOINT: ${{ secrets.MCP_SMOKE_ENDPOINT }}
          MCP_SMOKE_KEY: ${{ secrets.MCP_SMOKE_KEY }}
          MCP_SMOKE_TIMEOUT: "15"
        run: |
          set +e
          OUTPUT=$(bash ops/smoke/mcp-health.sh 2>&1)
          EXIT_CODE=$?
          set -e

          # Parse the single-line summary (last line of stdout)
          SUMMARY=$(echo "$OUTPUT" | tail -1)
          DETAILS=$(echo "$OUTPUT" | head -n -1)

          echo "exit_code=${EXIT_CODE}" >> "$GITHUB_OUTPUT"
          # Escape multiline for safe transport via GITHUB_OUTPUT
          echo "summary=${SUMMARY}" >> "$GITHUB_OUTPUT"

          if [[ $EXIT_CODE -eq 0 ]]; then
            echo "### ✅ MCP Healthy" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "${SUMMARY}" >> "$GITHUB_STEP_SUMMARY"
          elif [[ $EXIT_CODE -eq 3 ]]; then
            echo "### ⚠️ MCP Not Configured" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "${SUMMARY}" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "Set \`MCP_SMOKE_ENDPOINT\` and \`MCP_SMOKE_KEY\` repo secrets to enable this check." >> "$GITHUB_STEP_SUMMARY"
          elif [[ $EXIT_CODE -eq 1 ]]; then
            echo "### 🔴 MCP Auth Failure" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "${SUMMARY}" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "${DETAILS}" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          else
            echo "### ⚫ MCP Unreachable" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo "${SUMMARY}" >> "$GITHUB_STEP_SUMMARY"
            echo "" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "${DETAILS}" >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
          fi

          # Fail the job only on real failures (1=AUTH_FAIL, 2=UNREACHABLE).
          # Exit 3 (NOT_CONFIGURED) is a warning — job stays green.
          if [[ $EXIT_CODE -eq 1 || $EXIT_CODE -eq 2 ]]; then
            echo "::error::MCP smoke failed: ${SUMMARY}"
            exit 1
          fi

      - name: 📝 Create issue on failure
        if: failure()
        uses: actions/github-script@v7
        env:
          SMOKE_SUMMARY: ${{ steps.smoke.outputs.summary }}
          SMOKE_EXIT_CODE: ${{ steps.smoke.outputs.exit_code }}
        with:
          script: |
            const summary = process.env.SMOKE_SUMMARY || 'unknown';
            const exitCode = parseInt(process.env.SMOKE_EXIT_CODE || '0', 10);
            const state = exitCode === 1 ? 'AUTH_FAIL' : 'UNREACHABLE';
            const title = `🚨 MCP Smoke: ${state} — ${new Date().toISOString().slice(0,10)}`;

            // Check if there's already an open smoke issue to avoid duplicates
            const existing = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              labels: 'mcp-smoke,auto',
              per_page: 1,
            });

            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;

            if (existing.data.length > 0) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.data[0].number,
                body: `**Repeat failure** (${state})\n\n\`\`\`\n${summary}\n\`\`\`\n\nRun: ${runUrl}`,
              });
              return;
            }

            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title,
              body: `## MCP Smoke Test Failure\n\n**State:** ${state}\n**Summary:** \`${summary}\`\n**Workflow run:** [${context.runId}](${runUrl})\n\n### What this means\n\nThe automated MCP production smoke test detected that the endpoint is **${state.toLowerCase().replace('_', ' ')}**. This issue was auto-created.\n\n### Action required\n\n- [ ] Investigate MCP endpoint status\n- [ ] Fix the root cause\n- [ ] Close this issue once smoke test passes\n`,
              labels: ['mcp-smoke', 'auto', 'P1'],
            });
